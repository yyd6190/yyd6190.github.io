<% if (page.total > 1){ %>
  <nav class="pagination">
    <% if (page.prev){ %>
      <a class="prev" href="<%- url_for(page.prev_link) %>">上一页</a>
    <% } %>
    <% if (page.next){ %>
      <a class="next" href="<%- url_for(page.next_link) %>">下一页</a>
    <% } %>
  </nav>
<% } %>